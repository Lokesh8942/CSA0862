for num in range(1, 1001):
    org = num
    sum = 0
    while (org > 0):
        a = org % 10
        sum += a*a*a
        org //= 10
    if (num == sum):
        print(num, "is an Armstrong number")
